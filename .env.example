ANTHROPIC_API_KEY=
NEWS_API_KEY=
# Shared secret gating /deep/* and /review/resolve (deep analysis costs ~$1/run
# and the API has allow_origins=["*"]). Generate with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(24))"
SA_DEEP_TOKEN=
